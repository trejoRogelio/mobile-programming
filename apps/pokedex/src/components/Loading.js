import React from 'react';
import { ActivityIndicator } from 'react-native';

const Loading = () => {
  return <ActivityIndicator style={{ width: 'auto', height: 250 }} size='large' color='#E53939' />;
};

export default Loading;
